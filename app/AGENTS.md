# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

Nota (ronda 23): el proyecto se bajó de SDK 57 a SDK 54 porque Expo Go en el
App Store de iOS se quedó fijo en SDK 54 (Apple no aprobó actualizaciones
para SDK 55+; ver https://expo.dev/changelog/expo-go-and-app-store-may-2026).
No volver a subir el SDK a menos que se resuelva ese bloqueo o se migre a un
development build (EAS) en vez de Expo Go.
